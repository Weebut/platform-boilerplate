# platform-boilerplate/frontend

## Prerequisite

- .env.development (for local testing)
- .env (for production testing)

## Quick Start

```bash
/path/to/frontend % docker compose -f docker-compose.dev.yaml up -d
/path/to/frontend % docker compose up -d
```
